(function( $ ){
  $.julienLineCounter = {version: 0.1};

  $.julienLineCounter = function( jqSelector ) {
    var nLines = 0;
    var $elm = $(jqSelector);
    if ($elm.length == 0) { return false; }
    return nLines;
  };

})( jQuery );
